# lab-080: Network Troubleshooting with ss and tcpdump

QEMU VM for the LFCS course — diagnosing and fixing an unreachable port-8080 web application blocked by an nftables rule, using `ss`, `nft`, and `tcpdump`.

## Run

```bash
astrona run --git git@github.com:astrona-io/ATS003.git -c labs/lab-080
```

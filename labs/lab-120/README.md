# lab-120: firewalld Zones and Services: Runtime vs. Permanent Configuration

QEMU VM for the LFCS course — configuring firewalld zones, allowing a service and a custom port, and proving runtime and permanent configuration agree.

## Run

```bash
astrona run --git git@github.com:astrona-io/ATS003.git -c labs/lab-120
```

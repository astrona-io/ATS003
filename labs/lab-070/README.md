# lab-070: Bridge and Bonding Interfaces

QEMU VM for the LFCS course — building a persistent bridge (`br0`) and an active-backup bond (`bond0`) on dedicated dummy interfaces.

## Run

```bash
astrona run --git git@github.com:astrona-io/ATS003.git -c labs/lab-070
```

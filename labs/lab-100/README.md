# lab-100: Static Hostname Configuration

QEMU VM for the LFCS course — renaming a freshly provisioned host's static and pretty hostname with `hostnamectl` and keeping `/etc/hosts` in sync.

## Run

```bash
astrona run --git git@github.com:astrona-io/ATS003.git -c labs/lab-100
```

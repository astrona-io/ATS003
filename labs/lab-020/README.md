# lab-020: Packet Filtering, Port Redirection, and NAT with nftables

QEMU VM for the LFCS course — building nftables packet filters, a DNAT port redirect, and NAT rules on a single host.

## Run

```bash
astrona run --git git@github.com:astrona-io/ATS003.git -c labs/lab-020
```

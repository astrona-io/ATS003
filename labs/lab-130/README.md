# lab-130: Determining Your Real (Public) IP Address Behind NAT

QEMU VM for the LFCS course — finding a host's private interface address and its NAT-translated public IP via HTTP and DNS methods, recorded into `/opt/course/`.

## Run

```bash
astrona run --git git@github.com:astrona-io/ATS003.git -c labs/lab-130
```